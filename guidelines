# DTM Visualizer — 頁面 Layout 設計規範

## 1. 整體架構原則

- **Viewport 優先**：3D 地形畫面佔據全部可視區域，控制元件以 overlay 方式附著其上，不壓縮渲染空間。
- **預設隱藏，按需顯示**：非核心的控制面板預設收合，避免干擾地形瀏覽。
- **資訊貼近操作點**：滑鼠所在位置的資訊（座標、高程）顯示在靠近游標的角落，而非遠端面板。

---

## 2. 區域劃分

```
┌─────────────────────────────────────────────────────┐
│  [Logo / Title]                    [Panel Toggle]   │  ← Top Bar (40px)
├───────────────────────────────────────┬─────────────┤
│                                       │             │
│                                       │  Control    │
│           3D Viewport                 │  Panel      │
│           (主渲染區)                   │  (260px)    │
│                                       │             │
│                                       │  預設展開，  │
│                                       │  可收合      │
├───────────────────────────────────────┴─────────────┤
│  [座標資訊 HUD]            [圖例 / 色階]  [操作提示]  │  ← Bottom Bar (48px)
└─────────────────────────────────────────────────────┘
```

### 2.1 Top Bar
- 高度固定 40px，背景半透明（`rgba(0,0,0,0.6)`）
- 左：專案名稱（純文字，不需 logo 圖）
- 右：Control Panel 收合按鈕（`☰` / `✕`）

### 2.2 3D Viewport
- 佔滿 Top Bar 與 Bottom Bar 之間的全部寬高
- Control Panel 展開時，Viewport 寬度縮短（或以 overlay 方式覆蓋其上，不縮排）
- 背景色：深灰近黑（`#1a1a1e`），減少白色地形的對比衝擊

### 2.3 Control Panel
- 寬度 260px，靠右固定
- 背景半透明深色，支援滾動（內容超過視窗高時）
- 收合後僅留 40px 寬的 icon rail，保留快速存取

**Panel 內部分組（由上至下）：**

| 群組 | 內容 |
|------|------|
| 視角 | 重置相機、俯視、側視快捷按鈕 |
| 地形 | Z-scale 滑桿（0.5×～5×）、網格開關 |
| 色彩 | 色階選擇（高程漸層、灰階、彩虹）、透明度 |
| 圖層 | 未來擴充（衛星圖疊加、等高線）；預留空間即可 |

### 2.4 Bottom Bar
- 高度固定 48px，背景半透明
- 分三個區塊（等分或依重要性配比）：

**左 — 座標 HUD**
```
TWD97  E: 305,420 m  N: 2,771,080 m  ▲ 438.2 m
```
- 滑鼠移動時即時更新
- 無法取得時顯示 `—`

**中 — 色階圖例（Legend）**
```
0 m ━━━━━━━━━━━━━━━━━━━━━━━━ 3,952 m
```
- 水平漸層色條，左低右高
- 標示最低 / 最高高程值

**右 — 操作提示**
```
左鍵拖曳 旋轉  ｜  右鍵拖曳 平移  ｜  滾輪 縮放
```
- 首次進入顯示，互動後 3 秒後淡出，點擊可再顯示

---

## 3. 相機操作映射

| 輸入 | 動作 |
|------|------|
| 左鍵拖曳 | 繞地形中心 Orbit（旋轉） |
| 右鍵拖曳 / 中鍵拖曳 | Pan（平移） |
| 滾輪 | Zoom（縮放） |
| 雙擊地形 | 將點擊位置設為新的 Orbit 中心 |
| `R` 鍵 / 重置按鈕 | 回到預設視角（斜 45°，朝北偏東） |
| `T` 鍵 | 切換為俯視（Top-down） |

---

## 4. 初始視角

- 位置：地形 bounding box 中心偏北
- 仰角：30°～45°（可看見地形起伏，不純俯視）
- 方位：朝西南方看（台北盆地在視野中央）
- 距離：地形對角線的 0.8 倍（讓整個台北市場出現在畫面內）

---

## 5. 色彩設計

### 高程色階（預設）
從低至高建議使用地形慣用色系：

| 高程區間 | 顏色 |
|----------|------|
| 0 — 200m | 低地綠（`#4a7c59`） |
| 200 — 600m | 丘陵棕（`#8b6914`） |
| 600 — 1500m | 山地褐（`#6b4226`） |
| 1500m 以上 | 高山灰白（`#c8c8c8`） |

- NaN（無資料）區域：透明，不渲染三角面（已在轉換階段處理）
- 光照模型：Directional Light 從西北方 45° 射入，搭配環境光避免背面全黑

### 介面色彩
- 主色調：深色背景（`#1a1a1e`）
- 面板底色：`rgba(20, 20, 28, 0.85)`
- 文字：`#e0e0e0`（主）、`#888`（次要說明）
- 強調色：`#4fc3f7`（淡藍，用於 hover、選中狀態）

---

## 6. 響應式行為

| 視窗寬度 | 調整 |
|----------|------|
| ≥ 1280px | 預設展開 Control Panel |
| 768px — 1279px | Control Panel 預設收合，以 overlay 方式呈現 |
| < 768px（手機） | Panel 改為底部 Sheet，操作改為單指拖曳旋轉 |

---

## 7. 效能提示（非程式相關）

- 提供畫質切換：低（40m mesh）/ 高（20m mesh），讓低階裝置可選擇
- 首次載入時顯示 Loading 進度條（GLB 約 18–65 MB，需明確告知）
- 載入完成後自動播放一段緩慢的相機環繞動畫（約 3 秒），增加視覺衝擊感後停止
